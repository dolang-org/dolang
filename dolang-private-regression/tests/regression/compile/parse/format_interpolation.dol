echo "${:s}"
# error: invalid expression
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${:s}"
#    |          ^

echo "${42:5+}"
# error: invalid format specification
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:5+}"
#    |             ^

echo "${1.2:.}"
# error: expected precision after `.`
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${1.2:.}"
#    |             ^

echo "${42:q}"
# error: invalid format specification
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:q}"
#    |            ^

echo "${42:X}"
# error: invalid format specification
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:X}"
#    |            ^

echo "${42:$width<}"
# error: invalid format specification
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:$width<}"
#    |                  ^

echo "${42:4294967296}"
# error: format count is too large
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:4294967296}"
#    |                     ^

echo "${42:\u{3e}5}"
# error: escapes are only valid as the fill character in format specifications
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:\u{3e}5}"
#    |            ^^^^^^

echo "${42:\u{2b}d}"
# error: escapes are only valid as the fill character in format specifications
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:\u{2b}d}"
#    |            ^^^^^^

echo "${42:\u{7d}}"
# error: escapes are only valid as the fill character in format specifications
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:\u{7d}}"
#    |            ^^^^^^

echo "${42:\u{7d}\u{3e}5}"
# error: escapes are only valid as the fill character in format specifications
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:\u{7d}\u{3e}5}"
#    |                  ^^^^^^

echo "${42:*\u{3e}5}"
# error: escapes are only valid as the fill character in format specifications
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:*\u{3e}5}"
#    |             ^^^^^^

echo "${42:>\u{2b}d}"
# error: escapes are only valid as the fill character in format specifications
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:>\u{2b}d}"
#    |             ^^^^^^

echo "${42:>\u{23}x}"
# error: escapes are only valid as the fill character in format specifications
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:>\u{23}x}"
#    |             ^^^^^^

echo "${42:>\u{30}5}"
# error: escapes are only valid as the fill character in format specifications
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:>\u{30}5}"
#    |             ^^^^^^

echo "${42:>\u{35}}"
# error: escapes are only valid as the fill character in format specifications
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:>\u{35}}"
#    |             ^^^^^^

echo "${1.5:5\u{2e}2f}"
# error: escapes are only valid as the fill character in format specifications
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${1.5:5\u{2e}2f}"
#    |              ^^^^^^

echo "${1.5:.\u{32}f}"
# error: escapes are only valid as the fill character in format specifications
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${1.5:.\u{32}f}"
#    |              ^^^^^^

echo "${42:5\u{64}}"
# error: escapes are only valid as the fill character in format specifications
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo "${42:5\u{64}}"
#    |             ^^^^^^

echo b"${42:5}"
# error: formatted interpolation is not valid in binary strings
#   --> format_interpolation.dol:LL:LL
#    |
# LL | echo b"${42:5}"
#    |         ^
