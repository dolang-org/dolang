class Foo
  pub let x = 0
  pub let y = 0

  def (init) self x y
    self.x = x
    self.y = y

  pub def sum self
    (self.x + self.y)

let foo = Foo 1 2
assert_eq $foo.sum() 3
foo.(init) 3 4
assert_eq $foo.sum() 7
