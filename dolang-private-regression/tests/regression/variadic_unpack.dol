import std:
  - args

let rest = args 1 2 3 4
let a b ...rest = rest
assert_eq $a 1
assert_eq $b 2
let a b = rest
assert_eq $a 3
assert_eq $b 4

rest = args 1 foo: 42 2 bar: false
let a bar: b ...rest = rest
assert_eq $a 1
assert_eq $b false
let a foo: b = rest
assert_eq $a 2
assert_eq $b 42
