echo "${#0}"
# error: a parameter is only valid in a `t"..."` sequence, which keeps its segments apart to fill in later
#   --> fmt_param.dol:LL:LL
#    |
# LL | echo "${#0}"
#    |         ^

let doc = |-
  x ${#name}
# error: a parameter is only valid in a `t"..."` sequence, which keeps its segments apart to fill in later
#   --> fmt_param.dol:LL:LL
#    |
# LL |   x ${#name}
#    |       ^

echo t"${#-1}"
# error: parameter position must be a non-negative integer
#   --> fmt_param.dol:LL:LL
#    |
# LL | echo t"${#-1}"
#    |           ^^

echo t"${#}"
# error: expected a parameter name or number after `#`
#   --> fmt_param.dol:LL:LL
#    |
# LL | echo t"${#}"
#    |           ^

echo t"${#+}"
# error: expected a parameter name or number after `#`
#   --> fmt_param.dol:LL:LL
#    |
# LL | echo t"${#+}"
#    |           ^

echo t"${#0:q}"
# error: invalid format specification
#   --> fmt_param.dol:LL:LL
#    |
# LL | echo t"${#0:q}"
#    |             ^

echo "$#0"
# error: a parameter is only valid in a `t"..."` sequence, which keeps its segments apart to fill in later
#   --> fmt_param.dol:LL:LL
#    |
# LL | echo "$#0"
#    |        ^

echo t"$#"
# error: expected a parameter name or number after `#`
#   --> fmt_param.dol:LL:LL
#    |
# LL | echo t"$#"
#    |          ^
