import strand:
  - fork
let foo = nil
let bar = nil
let res = fork
  do foo = async 42
  do bar = async false
assert_eq $foo 42
assert_eq $bar false
assert_eq $res (Tuple([42, false]))

assert_eq (type res) $Tuple
assert_eq (fork()) (Tuple([]))
