def foo :bar = "hello"
  bar

assert_eq $foo() hello
assert_eq (foo bar: false) false

bind {}
  :foo = 42
assert_eq $foo 42

bind {foo: nil}
  :foo = 42
assert_eq $foo nil

bind record()
  :foo = 42
assert_eq $foo 42

bind record foo: nil
  :foo = 42
assert_eq $foo nil
