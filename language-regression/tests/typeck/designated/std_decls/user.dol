# A renamed import reaches the designated declaration by what it refers to.

import std:
  - @Int: Integer

pub let count @ Integer = 0
#               ^~~~~~~: ref std.Int

# An omitted ambient channel is gradual: its elements are `Unknown`.
pub def relay()
  #     ^~~~~: quantifier in @ std.Iter[Unknown] contravariant, out @ std.Sink[Unknown] contravariant
  nil

pub def drain <std.Iter[Integer]
  #     ^~~~~: quantifier out @ std.Sink[Unknown] contravariant
  nil
