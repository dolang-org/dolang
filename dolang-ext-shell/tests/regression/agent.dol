import sys

if (sys.os_info().family == :windows:)
  log "skipped on Windows"
  return

import agent:
  - with_agent

import proc.run:
  - echo

with_agent do
  let res = sub $echo hello
  assert_eq $res "hello"
