# CpuInfo

CPU information for the active VFS target returned by [`cpu_info`](./index.md).

## Fields

### `arch`

Target architecture. Supported values are `:X86_64:` and `:AARCH64:`.

### `logical_count`

Logical CPU count captured when the VFS context is established.
