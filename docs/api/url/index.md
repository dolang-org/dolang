# `url`

URL parsing and construction utilities.

## Types

### `Url`

See [`Url`](./url.md) for the full API reference.

`segments()` and `query()` each return a fresh iterator.

`Url` can be passed anywhere the HTTP extension accepts a URL string.
