import compile

let result = compile.compile "ok.dol" |-
  def foo
    1
  foo()

assert (type result compile.Result)
assert $result.ok
assert (type result.bytecode bin)
assert_eq $result.diagnostics.len 0
