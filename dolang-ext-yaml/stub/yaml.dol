# YAML serialization and deserialization.

# Parses a YAML string into a Do value.
#
# ## Returns
#
# The parsed Do value.
#
# ## Errors
#
# `ValueError` if the input is invalid, contains more than one document, or
# uses unsupported YAML features such as tags or aliases.
#
# | YAML Value | Do Type |
# | ---------- | ------- |
# | `null`     | `nil`   |
# | boolean    | `Bool`  |
# | integer    | `Int`   |
# | float      | `Float` |
# | string     | `Str`   |
# | sequence   | `array` |
# | mapping    | `dict`  |
#
# ## Example
#
# ```
# let doc = decode |
#   name: Alice
#   enabled: true
#   ports:
#     - 80
#     - 443
# assert_eq $doc["name"] "Alice"
# assert_eq $doc["enabled"] true
# assert_eq $doc["ports"] [80, 443]
# ```
pub def decode
  # ([`Str`](std.Str)) YAML document to parse.
  yaml
do
  throw nil

# Serializes a Do value to a YAML string.
#
# ## Returns
#
# [`Str`](std.Str) containing YAML.
#
# ## Errors
#
# `TypeError` if a value is not YAML-representable, such as binary data or a
# custom object.
#
# | Do Type | YAML Value |
# | ------- | ---------- |
# | `nil`   | `null`     |
# | `Bool`  | boolean    |
# | `Int`   | integer    |
# | `Float` | float      |
# | `Str`   | string     |
# | `Sym`   | string     |
# | `array` | sequence   |
# | `dict`  | mapping    |
#
# ## Example
#
# ```
# assert_eq (encode nil) "~"
# assert_eq (encode [1, 2, 3]) "- 1\n- 2\n- 3"
# ```
pub def encode
  # The value to serialize.
  value
do
  throw nil
